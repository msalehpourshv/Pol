{
  "name": "@pol/design-tokens",
  "version": "0.1.0",
  "main": "dist/tokens.js",
  "types": "dist/tokens.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json && mkdir -p dist && cp src/tokens.css dist/tokens.css"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
